import React from 'react';

const TitleForm = (): React.ReactElement => {
  return (
    <p
      style={{
        fontSize: 'xxx-large',
        marginBlockStart: '0.5rem',
        marginBlockEnd: '0.5rem',
      }}
    >
      Credit Card Validator
    </p>
  );
};

export default TitleForm;
